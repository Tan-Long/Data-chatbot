[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "wellness-commerce-api"
version = "0.1.0"
description = "FastAPI foundation for wellness commerce storefront, admin, and AI advisor."
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn[standard]>=0.30.0",
  "pydantic>=2.8.0",
  "pydantic-settings>=2.3.0",
  "psycopg[binary]>=3.2.0",
  "sqlalchemy>=2.0.30",
  "PyJWT>=2.9.0",
  "python-multipart>=0.0.9"
]

[project.optional-dependencies]
dev = [
  "httpx>=0.27.0",
  "pytest>=8.2.0"
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
